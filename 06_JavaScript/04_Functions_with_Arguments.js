// Argumnet = Values we pass to the function

// function funcName (arg1, arg2..) {
// //do something
// }

function printInfo(name, age) {
    console.log(`${name}'s age is ${age}.`);
}

printInfo("Imam", 22);

function sum(a, b) {
    console.log(a + b);
}
sum(1, 2);
sum(4, 5);
sum(7, 8);